#!/usr/bin/python3
if __name__ == "__main__":
    import sys
    total = 0
    if (len(sys.argv) > 1):
        for i, num in enumerate(argv):
            total += (int(num))
    print("{:d}".format(total))
